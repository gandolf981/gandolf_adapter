version: "3.9"

services:
  app:
    build: ./app
    container_name: telegram_worker
    restart: always

    tty: true
    ports:
      - "8000:8000"
    env_file:
      - ./app/.env
    volumes:
      - ./app:/app
